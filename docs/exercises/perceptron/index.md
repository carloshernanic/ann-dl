# Perceptron

!!! info inline end "Exercise"

    Perceptron — a single linear unit trained with the perceptron rule

    Sources: [`code/`](https://github.com/carloshernanic/ann-dl/tree/main/docs/exercises/perceptron/code) ·
    Figures: [`figures/`](https://github.com/carloshernanic/ann-dl/tree/main/docs/exercises/perceptron/figures)

!!! warning "Work in progress"

    This report has not been written yet. The sections below are the outline it
    will follow — the same one used in [Data](../data/index.md): what was asked,
    the code that was actually run, the figures it produced, and what they mean.

## Objective

*What is being asked, and why. What question does this exercise answer?*

## Data

*Which dataset, how it was generated or obtained, and any preprocessing applied.*

## Method

*The model, the training rule, and the hyperparameters used.*

Drop the scripts you actually ran into `code/`, then pull them into this page
instead of pasting the source twice:

```` markdown
``` python
--8<-- "docs/exercises/perceptron/code/perceptron.py"
```
````

## Results

*Figures produced by the code above, each with a caption, plus the metrics.*

## Discussion

*What worked, what did not, and what the results say about the limits of a
single linear unit.*
